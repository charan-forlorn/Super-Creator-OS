# Template — Bug Report

> Status: Approved (see [../governance/DOCUMENT_LIFECYCLE.md](../governance/DOCUMENT_LIFECYCLE.md))

Used at the start of [../playbooks/FIX_BUG.md](../playbooks/FIX_BUG.md).

---

**Repro steps:**
1. `<step>`
2. `<step>`

**Expected behavior:** `<what should happen>`

**Actual behavior:** `<what happens instead>`

**Affected files:** `<exact paths>`

**Production directory flag:** `<does this touch scos/, source/, memory/, assets/, analytics/, rendering/, workflow/, or tests/? yes/no — if yes, cross-check ../ai/ROUTING_RULES.md's production-touch rule before routing the fix>`

**Severity:** `<blocking / major / minor>`

**Routed model:** `<per ../ai/ROUTING_RULES.md — Debugging category>`
