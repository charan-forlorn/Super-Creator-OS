[pytest]
testpaths =
    integrations
    scos
    scripts
norecursedirs =
    .git
    .venv
    __pycache__
    work
cache_dir = scos/work/.pytest_cache
addopts = -m "not integration"
markers =
    integration: real-HVS / real-network acceptance tests; skipped by default collection
