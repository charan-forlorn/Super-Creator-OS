# Cross-Agent Workflow Router Contract

Purpose: deterministic local router that plans next-agent handoffs for Stage 5.6.

Scope: models, router logic, JSONL route store, static UI mock, and tests.

Non-goals: no external dispatch, no network, no API calls, no clipboard/browser automation.

See also AGENT_ROUTING_RULES_CONTRACT.md for rule table and allowed values.
