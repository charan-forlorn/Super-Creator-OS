
import * as childProcess from "node:child_process"; import * as fs from "node:fs"; import { dirname, join, resolve } from "node:path"; import type { IntakeResponse } from "./paid-pilot-intake-types";
const MODULE="scos.control_center.hvs_guided_pilot_intake_cli"; const MAX=1_048_576;
function python(){return process.env.SCOS_PYTHON_INTERPRETER&&process.env.SCOS_PYTHON_INTERPRETER.length>0?process.env.SCOS_PYTHON_INTERPRETER:resolve(process.cwd(),"..","..",".venv","Scripts","python.exe");}
function repoRoot(){const r=process.env.SCOS_REPO_ROOT; if(r) return r; let c=process.cwd(); for(let i=0;i<4;i++){if(fs.existsSync(join(c,"scos","control_center"))) return c; c=dirname(c);} return resolve(process.cwd(),"..","..");}
export function invokeIntake(operation:string,payload:Record<string,unknown>):Promise<IntakeResponse>{return new Promise((resolveOut)=>{const root=repoRoot(); const child=childProcess.spawn(python(),["-m",MODULE,operation],{cwd:root,env:{...process.env,PYTHONPATH:root,PYTHONIOENCODING:"utf-8",PYTHONDONTWRITEBYTECODE:"1",TZ:"UTC"},windowsHide:true}); let out=""; let done=false; const finish=(r:IntakeResponse)=>{if(done)return; done=true; resolveOut(r);}; const timer=setTimeout(()=>{try{child.kill("SIGKILL");}catch{} finish({ok:false,error_code:"BRIDGE_TIMEOUT",detail:"guided intake bridge timed out",draft:null});},60000); child.stdout.on("data",(b:Buffer)=>{out+=b.toString("utf8"); if(out.length>MAX){try{child.kill("SIGKILL");}catch{}}}); child.on("error",()=>{clearTimeout(timer); finish({ok:false,error_code:"BRIDGE_SPAWN_FAILED",detail:"guided intake authority unavailable",draft:null});}); child.on("close",()=>{clearTimeout(timer); try{const parsed=JSON.parse(out.trim()||"{}"); finish({ok:Boolean(parsed.ok),error_code:parsed.error_code??null,detail:parsed.detail??null,draft:parsed.draft??null,replay:parsed.replay,pilot_safe_id:parsed.pilot_safe_id,project_safe_id:parsed.project_safe_id,admission_packet_sha256:parsed.admission_packet_sha256,next_safe_action:parsed.next_safe_action});}catch{finish({ok:false,error_code:"BRIDGE_MALFORMED_OUTPUT",detail:"guided intake authority returned malformed output",draft:null});}}); child.stdin.write(JSON.stringify(payload)); child.stdin.end();});}
