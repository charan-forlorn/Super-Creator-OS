
import type { NextRequest } from "next/server"; import { NextResponse } from "next/server"; import { invokeIntake } from "@/lib/paid-pilot-intake-bridge";
export const dynamic="force-dynamic"; export const runtime="nodejs"; const MAX=32768; const OPS=new Set(["draft","update","consent","asset","sample-asset","brief-section","validate","create","get"]);
function safeError(code:string,detail:string,status=400){return NextResponse.json({ok:false,error_code:code,detail,draft:null},{status,headers:{"cache-control":"no-store"}});}
export async function POST(request:NextRequest){let raw=""; try{const b=await request.arrayBuffer(); if(b.byteLength>MAX) return safeError("REQUEST_TOO_LARGE","payload exceeds limit",413); raw=Buffer.from(b).toString("utf8");}catch{return safeError("REQUEST_UNREADABLE","body unreadable");} let body:Record<string,unknown>; try{body=JSON.parse(raw);}catch{return safeError("REQUEST_MALFORMED","invalid json");} const op=typeof body.operation==="string"?body.operation:""; if(!OPS.has(op)) return safeError("UNKNOWN_OPERATION",op); const payload=Object.fromEntries(Object.entries(body).filter(([key])=>key!=="operation")); const res=await invokeIntake(op,payload); return NextResponse.json(res,{status:res.ok?200:409,headers:{"cache-control":"no-store"}});}
